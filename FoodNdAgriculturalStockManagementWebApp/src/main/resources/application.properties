spring.application.name=FoodNdAgriculturalStockManagementWebApp
jwt.secret=mysupersecretkeymysupersecretkey
jwt.expiration=3600000   # 1 hour
# =========================
# SQL Server datasource
# =========================
spring.datasource.url=jdbc:sqlserver://localhost:1433;databaseName=FoodStockDB;encrypt=true;trustServerCertificate=true
spring.datasource.username=sa
spring.datasource.password=Gzna25i1i
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# =========================
# JPA / Hibernate
# =========================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# SQL Server dialect (auto-detect usually works, but we can be explicit)
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect
spring.web.resources.static-locations=classpath:/static/,file:uploads/
# allow up to 5MB; you can change to 10MB, etc.
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

